import React from 'react';

const book = {
  firstName: 'Nisha',
  lastName: 'Bharti',
  age: 30,
};

const sumOfTwoNumbers = (a, b) => {
  return a + b;
};

const PrettierTest = (arg1, agr2, agr3, arg4) => {
  return (
    <div>
      Hello Prettier
      <h1>"This is good house"</h1>
    </div>
  );
};

export default PrettierTest;
